"""raw -> staging. Type, standardize, and rename to a shared schema.

Target schema for every league:
    player_id, player_name, season, league, team, age,
    games, minutes, and per-100-possession rate stats

One column set for all four sources is what makes the rest of the project possible.
"""


def main() -> None:
    raise NotImplementedError


if __name__ == "__main__":
    main()
